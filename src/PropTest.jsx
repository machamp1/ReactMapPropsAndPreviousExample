function PropTest(props) {
  return (
    <div>
      {" "}
      <p> This is para one {props.paraoneTest}</p>
      <p> This is para two {props.paraTwoTest}</p>
      <p> This is para three {props.ParaThreeTest}</p>
    </div>
  );
}

export default PropTest;
